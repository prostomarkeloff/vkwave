[tool.poetry]
name = "vkwave-types"
version = "0.0.1"
description = "Vk types"
authors = ["kesha1225"]

[tool.poetry.dependencies]
python = "^3.7"
pydantic = "^1.4"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
